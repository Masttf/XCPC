#include<iostream>
#include<vector>
using namespace std;
vector<vector<int>>res(2);
vector<int>number;
vector<int>answer;
vector<bool>track;
void dfs(int x,int start,){
	if()
}
int main(){
	string n;
	int k;
	cin>>n>>k;
	int x,y;
	for(int i=0;i<k;i++){
		cin>>x>>y;
		res[0].push_back(x);
		res[1].push_back(y);
	}
	for(int i=0;i<n.size();i++){
		dfs(n[i]-'0',)
	}
	return 0;
}
